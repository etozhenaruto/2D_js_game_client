import { Link } from 'react-router-dom';

export const RoomNotFind = () => {

    return (
        <div>
            <h1>Room not find</h1>

            <Link to="/">back to search</Link>
        </div>
    );
}